<div class="col-md-4 col-md-offset-4">
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <% if devise_error_messages! %>
      <p class="notice alert-info text-center"><%= devise_error_messages! %></p>
      <% end %>
  <div class="panel panel-white load1">
    <div class="panel-heading border-light">
      <div class="logo text-center">
        <img src="/LAYOUT-4/STANDARD/assets/images/logo.png" alt="OQBA Boekhouderskantoor Rotterdam" width="40%" />
      </div><hr/>
      <h4 class="panel-title text-center"><span class="text-bold">Registreren</span></h4>
    </div>
    <div class="panel-body">
      <div class="box-register">
        <form class="form-register">
          <div class="form-group">
            <%= f.label :email %><br />
            <%= f.email_field :email, class: 'form-control', autofocus: true %>
          </div>
          <div class="form-group">
            <%= f.label 'wachtwoord' %>
            <% if @minimum_password_length %>
                <em>(Minimaal <%= @minimum_password_length %> tekens)</em>
            <% end %><br />
            <%= f.password_field :password, class: 'form-control', autocomplete: "off" %>
          </div>
          <div class="form-group">
            <%= f.label 'Herhaal wachtwoord' %><br />
            <%= f.password_field :password_confirmation, class: 'form-control', autocomplete: "off" %>
          </div>
          <div class="form-group">
            <div class="checkbox clip-check check-primary">
              <input type="checkbox" id="agree" name="agree" value="agree">
              <label for="agree"> Ik ga akkoord met de voorwaarden </label>
            </div>
          </div>
          <div class="form-actions">
            <%= f.submit "Registreren", class: 'btn btn-red btn-block' %>
          </div><br/>
          <p class="text-small">
            Heeft u al een account? - <%- if controller_name != 'sessions' %>
                <%= link_to "Inloggen", gebruiker_inloggen_path %><br />
            <% end -%>
          </p>
   <!--       <p class="margin-top-10 text-center">
            Or sign up with a social network
          </p>
          <div class="social-icons text-center">
            <ul>
              <li class="social-twitter" uib-tooltip="Twitter" tooltip-placement="top">
                <a target="_blank" href="#"> Twitter </a>
              </li>
              <li class="social-google" uib-tooltip="Google" tooltip-placement="top">
                <a target="_blank" href="#"> Google </a>
              </li>
              <li class="social-facebook" uib-tooltip="Facebook" tooltip-placement="top">
                <a target="_blank" href="#"> Facebook </a>
              </li>
            </ul>
          </div> -->
        </form>
      </div>
    </div>
  </div>
</div>
<% end %>

