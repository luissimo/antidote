<%= form_for(@factuur) do |f| %>
  <% if @factuur.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@factuur.errors.count, "error") %> prohibited this factuur from being saved:</h2>

      <ul>
      <% @factuur.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :currency %><br>
    <%= f.text_field :currency %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
